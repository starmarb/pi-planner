{
  "name": "pi-growth-prototype",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild main.jsx --bundle --loader:.jsx=jsx --jsx=automatic --minify --outfile=docs/bundle.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
